.chat-side
  .side-header
    .settings
      .settings__name
        = current_user.name
      %ul.settings__icon
        %li.settings__icon__new
          = link_to new_group_path, class: "icon" do
            = fa_icon "edit"
        %li.settings__icon__setting
          = link_to edit_user_path(current_user), class: "icon" do
            = fa_icon "cog"
  .groups
    - current_user.groups.each do |group|
      = link_to group_messages_path(group), class: "group" do
        .group__name
          = group.name
        .group__message
          まだメッセージがありません。
